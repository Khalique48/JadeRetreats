/** @type {import('next').NextConfig} */
// next.config.js
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "unsplash.com", "drive.google.com"],
  },
};

module.exports = nextConfig;
